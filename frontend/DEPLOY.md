# Инструкция по деплою фронтенда

## 🚀 Самый простой способ: Vercel (рекомендуется)

### Шаг 1: Подготовка
1. Убедитесь, что ваш код находится в Git репозитории (GitHub, GitLab, Bitbucket)

### Шаг 2: Деплой через Vercel
1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub/GitLab/Bitbucket
3. Нажмите "Add New Project"
4. Выберите ваш репозиторий
5. Настройки проекта:
   - **Framework Preset**: Vite
   - **Root Directory**: `frontend` (если фронтенд в подпапке)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
6. Добавьте переменные окружения (если нужно):
   - `VITE_API_URL` - URL вашего бэкенда (например: `https://your-backend.com`)
   - `VITE_USE_MOCK` - `false` для продакшена
7. Нажмите "Deploy"

✅ Готово! Vercel автоматически задеплоит ваш проект и даст вам URL.

### Автоматический деплой
После первого деплоя, каждый push в основную ветку автоматически обновит сайт.

---

## 🌐 Альтернатива: Netlify

### Через веб-интерфейс:
1. Перейдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub
3. Нажмите "Add new site" → "Import an existing project"
4. Выберите репозиторий
5. Настройки:
   - **Base directory**: `frontend`
   - **Build command**: `npm run build`
   - **Publish directory**: `frontend/dist`
6. Добавьте переменные окружения в разделе "Site settings" → "Environment variables"
7. Нажмите "Deploy site"

### Через Netlify CLI:
```bash
npm install -g netlify-cli
cd frontend
netlify login
netlify deploy --prod --dir=dist
```

---

## 📦 Локальная сборка для деплоя

```bash
cd frontend
npm install
npm run build
```

Собранные файлы будут в папке `frontend/dist/`

---

## 🔧 Переменные окружения

Создайте файл `.env.production` в папке `frontend/`:

```env
VITE_API_URL=https://your-backend-api.com
VITE_USE_MOCK=false
```

---

## 🎯 Другие варианты деплоя

### GitHub Pages
1. Установите `gh-pages`: `npm install -D gh-pages`
2. Добавьте в `package.json`:
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```
3. Запустите: `npm run deploy`

### Cloudflare Pages
1. Перейдите на [pages.cloudflare.com](https://pages.cloudflare.com)
2. Подключите репозиторий
3. Настройки аналогичны Vercel/Netlify

---

## ⚠️ Важные замечания

1. **CORS**: Убедитесь, что ваш бэкенд разрешает запросы с домена фронтенда
2. **API URL**: В продакшене используйте полный URL бэкенда в переменной `VITE_API_URL`
3. **HTTPS**: Все современные платформы деплоя используют HTTPS по умолчанию

